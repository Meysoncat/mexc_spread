.venv/
venv/
__pycache__/
*.pyc
.streamlit/secrets.toml
node_modules/
frontend/node_modules/
frontend/dist/
data/
!frontend/src/data/
*.sqlite

# Secrets — never commit env files or local credentials.
.env
.env.*
!.env.example
!.env.docker.example

# Local tooling / IDE state.
.zcode/
.idea/
.vscode/

# Skill runtime artifacts (web-user-sim screenshots, reports, debug scripts).
.web-user-sim/

# Hummingbot research scrape (one-off, see docs/COMPETITORS.md).
hb-out/
scrape_hummingbot.py

# Logs.
*.log
api*.log
_run_modern*.log
frontend/ui*.log

# Ad-hoc debug / scratch files at repo root.
_debug*.png
_selector*.png
_cdp_*.py
_cdp_*.json

# Ad-hoc snapshots / patches not meant for the repo.
frontend_src_snapshot.zip
mexc_spread_*.patch
mexc_spread_iteration_*.patch

# Scratch files from other AI tooling (Kimi webbridge etc.) — not ours.
patch_backend.py
screenshot*.json
snap*.json
snapshot*.json
wb-*.json
webbridge-*.json

# Local roadmap drafts not yet promoted into docs/.
mexc_spread_roadmap.md

# Research scratch (one-off investigations, not project docs).
research/
docs/HUMMINGBOT_*.md

# One-off analysis / scrape scripts (see file headers).
analyze_trades.py
scrape_proboyscalp.py

# Xray proxy bridge artifacts (binary + generated config with VLESS UUID/secret).
# See scripts/xray_bridge.py / docs/PROXY_XRAY.md — never commit these.
.xray/

# Local config (machine-specific client config, skills index with absolute paths).
conf/
.agents/skills.json

# Downloaded generic agent skills — project skill web-user-sim stays tracked.
.agents/skills/*
!.agents/skills/web-user-sim/

_cm.txt  
