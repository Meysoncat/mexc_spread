# Приоритизированный Roadmap — MEXC Spread Monitor

> Составлено на основе анализа `mexc_spread_roadmap.md` и архитектуры проекта.  
> Принцип: **сначала чиним то, что мешает пользоваться → потом фундамент для всего остального → потом фичи, дающие конкурентное преимущество.**

---

## 🔥 ЭТАП 0: Hotfix (1–2 дня) — «таблицу можно читать»

Задачи, которые мешают даже базовому использованию. Максимальный эффект за минимальное время.

| # | Задача | Источник | Почему первым |
|---|--------|----------|---------------|
| 0.1 | **Иконки по hover** — тикер в таблице не виден из-за 6 наложенных иконок | 0.1 | Базовая читаемость таблицы |
| 0.2 | **Sticky-колонка символа** — при скролле вправо не видно, чья строка | 0.2 | 10 минут работы, критично для таблицы с 14 колонками |
| 0.4 | **Дефолтные фильтры и сортировка** — сейчас топ = мусор (спред 5000 bps, объём $25) | 0.4 | Пользователь открывает → видит мусор → уходит |
| 0.6 | **Empty states и человекочитаемые ошибки** — «Арбитраж» с вечными скелетонами, сырые стектрейсы в UI | 0.6 | Пользователь не понимает, сломано ли что-то или просто не запущено |
| 0.10 | **Скрывать неприменимые колонки** (funding в споте), компактный режим строк | 0.10 | ~10 минут, заметно больше данных на экране |

---

## 🏗️ ЭТАП 1: Фундамент UI (3–5 дней) — «графики и внешний вид»

Без этих задач каждая новая фича будет делаться на глиняных ногах (7 разных реализаций графиков, разные темы).

| # | Задача | Источник | Почему сейчас |
|---|--------|----------|---------------|
| 6.1 | **Единый компонент `<TradingChart>`** — обёртка над lightweight-charts для всех 7 мест | 6.1 | Фундамент. Без него каждый график — отдельный костыль. Добавлять новые фичи на график = 7 раз |
| 6.2 | **Тёмная «терминальная» тема по умолчанию** — как Wintrading/TradingView | 6.2 | Меняет восприятие продукта с «хобби-проекта» на «профессиональный терминал». Делается вместе с 6.1 |
| 0.3 | **Сворачиваемая панель фильтров** — освободить ~260px для таблицы | 0.3 | Таблица сейчас занимает <50% экрана. Критично для работы со спредами |
| 0.5 | **Страница алертов** — минимум empty-state + инструкция + список правил | 0.5 | Сейчас полностью пустая страница — выглядит как баг |
| 0.9 | **Индикатор фидов в шапке** — «WS live / REST / кэш 3с» | 0.9, 1.4 | Пользователь должен видеть, свежие ли данные (особенно после гео-блоков MEXC) |
| 6.3 | **Пресеты оформления графиков** — 5 встроенных тем + кастомизация | 6.3 | Логичное продолжение 6.1+6.2, механический перенос |

**Зависимость:** 6.1 → 6.2 → 6.3 (последовательно, ~3-4 дня суммарно).

---

## 🎯 ЭТАП 2: Карточка сделки (1–2 недели) — «главный UI-разрыв с конкурентами»

Это то, чего нет у конкурентов в таком виде, и что превращает «таблицу чисел» в «торговый инструмент».

| # | Задача | Источник | Почему сейчас |
|---|--------|----------|---------------|
| 1.1 | **Карточка сделки** — клик по строке → единый экран: net-спред обеих ног, стаканы бок о бок, funding, история, калькулятор объёма (vwap.py уже считает, нужен только UI) | 1.1, 5.1, 5.2 | Объединяет разрозненные данные (спред в таблице, стакан в DomModal, график в ChartModal). **Главный разрыв с Wintrading/Coinglass** |
| 5.1 | **Строка = готовая сделка в $** — глобальное поле «Position size $» в шапке → колонки «PnL $», «APY %» от размера позиции | 5.1 | Пользователь мыслит долларами, не bps. У Coinglass так — и это работает |
| 5.2 | **Страница монеты как хаб** — KPI-карточки → график → таблица по биржам. Связка с 1.1 | 5.2 | Даёт URL на каждую монету (усиливает 2.2) |
| 1.2 | **Агрегированный стакан по монете** — лимитки всех бирж на одном экране с суммарной плотностью | 1.2 | У Wintrading и P2P.Army это флагманская фича. Данные уже идут по WS |
| 0.7 | **Единая дизайн-система** — таблицы, табы, кнопки, статусы в едином стиле | 0.7, 1.3 | Делать вместе с карточкой, чтобы не переделывать 2 раза. 10 страниц сейчас выглядят как 10 разных приложений |

**Зависимость:** 6.1+6.2 (единый график + тема) → 1.1 (карточка использует графики). Если делать 1.1 до 6.1 — придётся переделывать.

---

## 📊 ЭТАП 3: Метрики, которым можно верить (1–2 недели) — «отсечь шум»

Сейчас скринер показывает много «возможностей», которые на самом деле нереализуемы. Это подрывает доверие к продукту.

| # | Задача | Источник | Почему сейчас |
|---|--------|----------|---------------|
| 3.2 | **Исполнимый объём вместо L1** — «сколько $ можно исполнить внутри X bps slippage» (vwap.py уже есть) | 3.2, 5.1 | Сейчас L1 часто 0 на фьючерсах → пользователь не понимает ликвидность. Сортировка по «спред × исполнимый объём» = реальный рейтинг заработка |
| 4.1 | **Единый net-спред для всех стратегий** — net = грязный спред − taker-fee обеих ног − slippage ± funding | 4.1 | Сейчас каждая страница считает по-своему. Это фундамент для скринера, алертов и калькулятора |
| 3.1 | **Время жизни спреда (lifetime)** — сколько секунд спред держится выше порога, частота за 24ч | 3.1 | Отсекает разовый шум от стабильных возможностей. Есть у ArbScanner/P2P.Army, нет у вас |
| 3.3 | **Проверка deposit/withdraw и общих сетей** — колонка «переводимо: да/нет/сеть» | 3.3 | Без этого половина кросс-биржевых сигналов нереализуема. Публичные эндпоинты есть, кэшировать раз в 10-15 мин |
| 4.2 | **Модель схождения спреда (mean reversion)** — ожидаемое время удержания, вероятность схождения за T | 4.2 | Сейчас сигнал статичен («спред > порога»). По истории можно оценить, сколько в среднем держится — это решает, входить или нет |

**Зависимость:** 4.1 (единый net-спред) → 3.2 (исполнимый объём в $), 4.2 (модель схождения). Без единой формулы каждая фича считает по-своему.

---

## 🔔 ЭТАП 4: Удобство и алерты (3–5 дней) — «UX-дополнения»

Быстрые задачи, которые заметно повышают удобство. Можно делать параллельно с Этапом 3.

| # | Задача | Источник | Эффект |
|---|--------|----------|--------|
| 2.1 | **Алерт по клику** — клик по спреду/funding → «уведомить, когда > X» (порог = текущее значение) | 2.1 | У Watchlist.top так — очень удобно. Сейчас алерты — отдельная страница с формами |
| 2.2 | **Состояние в URL** — фильтры, сортировка, символ в query-параметрах | 2.2, 5.2 | Можно делиться ссылкой, букмаркать. Для мультимониторных сетапов критично |
| 5.3 | **Глобальный поиск «/»** — из любого места, ведёт на страницу монеты | 5.3 | Дёшево и заметно. У Coinglass/Watchlist.top так |
| 2.5 | **Горячие клавиши** — `/` поиск, стрелки навигация, Enter = карточка | 2.5, 5.6 | Профессиональный терминал без хоткеев — не терминал |
| 1.4 | **Индикатор свежести данных в шапке** — «WS live / REST / кэш N с» для каждой биржи | 1.4, 0.9 | freshness.py уже считает, нужен только UI. Сразу видно, что отвалилось |
| 2.3 | **Пресеты фильтров** — «широкие спреды», «высокий funding», «мои монеты» | 2.3, 5.8 | У Watchlist.top и Wintrading — пресеты от сценария торговли |

---

## 🧠 ЭТАП 5: Модели и торговля (2–3 недели) — «перед live»

Это задачи, которые нужны для того, чтобы торговать по сигналам, а не просто смотреть. Делать **после** стабильной работы скринера.

| # | Задача | Источник | Почему после скринера |
|---|--------|----------|----------------------|
| 4.4 | **Backtest на кросс-спредах** — вход при net-спреде > X, выход по схождению/тайм-ауту, с комиссиями и funding | 4.4 | **Главный инструмент проверки порогов до live**. Сейчас backtest.py только для SpreadCapture |
| 4.3 | **Калибровка execution_model** — paper-журнал против реальных aggTrades, калибровка λ per-symbol | 4.3 | Иначе paper-PnL систематически завышен → неправильные ожидания при переходе в live |
| 4.5 | **Kelly / риск-бюджет** — рекомендация размера позиции из winrate/payoff по истории | 4.5 | PortfolioRiskManager уже держит лимиты — нужно только добавить формулу сайзинга |
| 3.4 | **Funding-метрики уровня Coinglass** — накопленный funding 7/30 дней, predicted funding | 3.4 | Данные частично собираются, нужна агрегация и история |
| 3.5 | **NATR, всплеск объёма, изменение OI** — для отбора «живых» монет | 3.5 | NATR по klines уже доступен, OI — по REST раз в минуту |
| 4.6 | **Lead-lag → торговый сигнал** или удалить из навигации | 4.6, 1.6 | Если не используется — не держать мёртвый груз |

---

## 🧹 ЭТАП 6: Чистка и консолидация (постоянно, фоном)

Убираем то, что не работает или не используется, чтобы не тянуть technical debt.

| # | Задача | Источник | Почему |
|---|--------|----------|--------|
| 4.8 | **Удалить мёртвый DNS-слой** (`network/`, `CustomDNSHTTPTransport`) | 4.8 | Не работает, вводит в заблуждение, создаёт ложные ошибки |
| 1.6 | **Убрать AsterDexPage и LeadLagPage** из основной навигации | 1.6 | Aster — просто ещё одна биржа, влить в Мультибиржу. LeadLag — мёртвый груз без торговой стратегии |
| 3.7 | **Убрать грязный кросс-спред как основную сортировку** | 3.7 | После net-спреда грязный оставить только как вспомогательную колонку |
| 4.8 | **Удалить/довести неиспользуемые интеграции бирж** (`lighter/`, др.) | 4.8 | Каждая полуживая интеграция — источник ложных ошибок в логах |
| 1.3 | **Консолидация страниц** — из 10 в 3-4 базовых экрана | 1.3, 5.7 | Делать вместе с карточкой сделки (1.1), чтобы не переделывать 2 раза |

---

## 📋 Итоговая таблица приоритетов

| Этап | Срок | Задачи | Критерий готовности |
|------|------|--------|---------------------|
| **0. Hotfix** | 1–2 дня | 0.1, 0.2, 0.4, 0.6, 0.10 | Таблица читаема, нет пустых страниц, дефолтные фильтры осмысленны |
| **1. Фундамент UI** | 3–5 дней | 6.1, 6.2, 6.3, 0.3, 0.5, 0.9 | Все графики на едином компоненте, тёмная тема, сворачиваемые фильтры |
| **2. Карточка сделки** | 1–2 недели | 1.1, 5.1, 5.2, 1.2, 0.7 | Клик по строке → единый экран сделки со стаканами и графиком |
| **3. Метрики** | 1–2 недели | 3.2, 4.1, 3.1, 3.3, 4.2 | Скринер показывает только реализуемые возможности (исполнимый объём, lifetime, net-спред) |
| **4. Удобство** | 3–5 дней | 2.1, 2.2, 5.3, 2.5, 1.4, 2.3 | Алерты по клику, URL-состояние, глобальный поиск, хоткеи |
| **5. Модели + Live** | 2–3 недели | 4.4, 4.3, 4.5, 3.4, 3.5 | Backtest кросс-спредов, калибровка paper vs live, Kelly-сайзинг |
| **6. Чистка** | Фоном | 4.8, 1.6, 3.7, 1.3 | Нет мёртвых страниц, нет ложных ошибок в логах |

---

## ⚠️ Ключевые зависимости

1. **6.1 (единый график) → 1.1 (карточка сделки)**. Если делать карточку раньше — придётся переписывать графики в ней.
2. **4.1 (единый net-спред) → 3.2 (исполнимый объём $), 4.2 (модель схождения), 4.4 (backtest)**. Без единой формулы каждый модуль считает по-своему.
3. **3.2 (исполнимый объём) → 5.1 (PnL в $)**. Нельзя показать «сколько заработаешь на $1000» без знания, сколько исполнится.
4. **1.1 (карточка) → 1.3 (консолидация страниц)**. Консолидировать страницы логично вместе с внедрением карточки, а не до.

---

## 🎯 Что делать прямо сейчас

Если сегодня есть 1 день: **0.1 + 0.2 + 0.4** — таблица станет читаемой и покажет осмысленные данные.

Если есть 1 неделя: добавить **6.1 + 6.2** — все графики единообразны, тёмная тема, продукт выглядит профессионально.

Если есть 2 недели: добавить **1.1** — карточка сделки, и продукт становится функциональным аналогом Wintrading/Coinglass.

Если есть 1 месяц: дойти до конца **Этапа 3** — и скринер будет показывать только реализуемые, обоснованные сигналы.
