{
  "name": "mexc-spread-monitor",
  "private": true,
  "scripts": {
    "dev:modern": "concurrently -k -n api,ui -c cyan,magenta \"node scripts/uvicorn-cli.cjs\" \"npm run dev --prefix frontend -- --host\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}